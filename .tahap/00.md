# inisialisasi project
> npx create-expo-app --template
  ---- [Blank] JS

rm -rf node_modules package-lock.json
npm cache clean --force
npm install
npx expo install --check

# Instal dependensi utama
npx expo install expo-sqlite

# Instal tools untuk development
npm install drizzle-orm@latest && npm install -D drizzle-kit@latest

-----------

# Menyiapkan skema
/db
  ├── schema.js       <-- Definisi tabel
  ├── client.js       <-- Koneksi database
  └── dao/            <-- Logika Query (Data Access Object)
      ├── ProdukDAO.js
      └── LogDAO.js

npx drizzle-kit generate

npx expo install expo-crypto

npx expo install @shopify/flash-list

================================================================
================================================================

### [ETC]
npm update
npm ls glob

